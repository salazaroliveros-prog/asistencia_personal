rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // La aplicación usa Authentication anónima. Publica estas reglas después
    // de habilitar Anonymous Authentication en Firebase Console.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
