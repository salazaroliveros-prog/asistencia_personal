feat: puesto de trabajo personalizado en formulario de personal

- field-scanner.js: refactor de autenticación — email_verified (cualquier
  usuario verificado puede operar), localStorage persiste email + perfil,
  toggle de visibilidad de contraseña, logout limpia todo.
- js/modules/personal.js: +5 funciones puesto personalizado
  (_esPuestoBase, _leerPuestosCustom, _guardarPuestosCustom,
  _registrarPuestoCustom, _resolverPuesto, _setPuestoSelect,
  _sincronizarOpcionesPuesto, _togglePuestoCustom, _initPuestosCustom).
  Presisten puestos en localStorage (cpc_puestos_custom), inyectan en #p-puesto
  y #filter-puesto. Enter/Salida valida, Escape anula.
- index.html: option __custom__ + input p-puesto-custom en formulario.
- css/components.css: estilos del puesto personalizado (animación, foco, error).
- js/utils/constants.js: +PUESTOS_BASE.
- firestore.rules: canMarkAttendance = isAuthenticated (abierto).
- README.md: documentación de autenticación + puestos personalizados.
- Tests: +5 unit tests, +6 E2E puesto-personalizado.spec.ts, +6 E2E app-integral.

All: 116 unit tests ✓, 12 E2E ✓, build ✓, Vercel ✓
